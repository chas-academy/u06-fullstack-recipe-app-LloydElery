import { RecipeIdFormatterPipe } from './recipe-id-formatter.pipe';

describe('RecipeIdFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeIdFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
